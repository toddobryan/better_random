name: better_random
description: A library for better random number generation
version: 0.1.0
repository: https://github.com/toddobryan/better-random
publish_to: none

environment:
  sdk: ^3.11.3

dependencies:
  sized_ints:
    path: ../sized_ints
  checks: ^0.3.1
  fast_immutable_collections: ^11.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
